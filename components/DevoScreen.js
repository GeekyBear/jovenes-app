const { View, Text } = require("react-native")

const DevoScreen = ({ navigation }) => {
    return (
        <View>
            <Text>Devo Screen</Text>
        </View>
    )
}

module.exports = { DevoScreen }